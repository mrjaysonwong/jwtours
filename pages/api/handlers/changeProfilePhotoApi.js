import User from '@model/userModel';

// continue with post method
// get userId from query
// get the cloudinary upload api secure_url in results.info
// query mongoose User model findByIdAndUpdate
// store the secure_url in mongodb user object { image: "" }

export default async function changeProfilePhotoApi(req, res) {
  try {
  } catch (error) {
    
  }
}
